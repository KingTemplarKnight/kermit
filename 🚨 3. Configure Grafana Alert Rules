🚨 3. Configure Grafana Alert RulesTo prevent transaction block drops, monitor system thresholds using these targeted alert metrics.GoChain Connection FailuresMetric Formula: gochain_rpc_connection_status == 0Condition: Trigger immediately if connection equals 0 for over 2 minutes.Action: Fire a warning alert to dispatch a hotfix runner to recover the blockchain RPC link.Pipeline Memory SaturationMetric Formula: container_memory_working_set_bytes{container="execution-container"} > 858993459Condition: Trigger if memory utilization exceeds 80% of your 1Gi resource limit for more than 5 minutes.Action: Signal an optimization leak inside the BlockLang compiler engine.
